# before start project please create file "api.config.yml" and paste this configuration
db:
  mysql:
    host: localhost
    user: root
    password: admin
    database: run
    connectionLimit: 10
server:
  host: localhost
  port: 5001
redis:
  host: localhost
  port: 6379